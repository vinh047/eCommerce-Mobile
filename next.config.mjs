/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "fontawesome.com",
            "upload.wikimedia.org",
            "www.wikipedia.org",
        ],
    },
};

export default nextConfig;
