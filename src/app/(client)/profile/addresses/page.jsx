import React from "react";
import AddressPageClient from "./_components/AddressPageClient";

export const metadata = {
  title: "Sổ địa chỉ | VNShop",
};

export default function AddressPage() {
  return <AddressPageClient />;
}
